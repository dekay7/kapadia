{
  "name": "kapadia-site",
  "private": true,
  "type": "module",
  "scripts": {
    "render": "node scripts/check-sync.js && node scripts/render.js",
    "build": "npm run render",
    "dev": "npm run render && wrangler pages dev public --port 8788"
  },
  "devDependencies": {
    "marked": "^16.3.0"
  },
  "dependencies": {
    "wrangler": "^4.90.1"
  }
}
